import styled from 'styled-components'

export const StyledUserCard = styled.div`
  display: flex;
  align-items: center;

  div:first-child {
    flex: 0 0 auto;
  }

  div:last-child {
    margin-left: 0.5rem;
  }
`
